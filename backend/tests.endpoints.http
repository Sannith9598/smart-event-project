### Smart Event Backend quick check (REST Client)

# 1) Register a manager (safe to run even if user exists — will return error)
POST http://localhost:5000/api/auth/register
Content-Type: application/json

{
  "name": "MgrREST",
  "email": "mgrrest@example.com",
  "password": "MgrPass123",
  "role": "MANAGER",
  "location": "Bangalore"
}

###

# 2) Login (Manager) — run this AFTER register (or if already registered)
POST http://localhost:5000/api/auth/login
Content-Type: application/json

{
  "email": "mgrrest@example.com",
  "password": "MgrPass123"
}

###

# NOTE: After step (2) copy the returned "token" value and paste it into the Authorization header below.
# 3) Protected route test (use Authorization header: Bearer <token>)
GET http://localhost:5000/api/protected
Authorization: Bearer <token>
###

# 4) Create an event (manager only) — update Authorization with token before sending
POST http://localhost:5000/api/events
Content-Type: application/json
Authorization: Bearer <token>

{
  "title": "REST Client Event",
  "description": "Created via REST Client test",
  "category": "Demo",
  "location": "Bangalore",
  "price": 1500,
  "gallery": [
    "https://res.cloudinary.com/df4q8ylv7/image/upload/v1764352674/smart_event_test/l5pul4rilpkt8cjd1lrk.jpg"
  ]
}

###

# 5) Get public events (no auth required)
GET http://localhost:5000/api/events

###

# 6) Optional: Protected route showing how to pass JWT for curl/Postman tests
# (This is an example header only; not executed in REST file)
# Authorization: Bearer <PASTE_MANAGER_TOKEN_HERE>

